include ../../node_modules/bempug/index
- BEMPUG.modifier = '_';
include ./../blocks/head/head


doctype html
html(lang="en")
  +head({
    title: 'Список страниц'
  })
  style .header{background: #000; padding:30px; color: #fff; font-Weight:bold;}.list{padding:30px; list-style: circle;}

  body
    +b('header',{},'header')
      | Cписок страниц

    +b('list',{},'ul')
      each item in linkslist
        +e('item')
          +e('link',{},'a')(href=item.link target="_blank")= item.title
